[project]
name = "oxzoo-vue-django"
version = "0.1.0"
description = "ox deploy example: Django API + Vue 3 SPA + celery heartbeat on one VPS"
requires-python = ">=3.13"
dependencies = [
    "celery[redis]==5.6.3",
    "dj-database-url==3.1.2",
    "django==5.2.17",
    "gunicorn==23.0.0",
    "psycopg[binary]==3.3.5",
    "redis==6.4.0",
]

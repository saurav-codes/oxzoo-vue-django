[project]
name = "oxzoo-vue-django"
version = "0.1.0"
description = "ox deploy example: Django API + Vue 3 SPA"
requires-python = ">=3.13"
dependencies = [
    "django==5.2.17",
    "gunicorn==23.0.0",
]
